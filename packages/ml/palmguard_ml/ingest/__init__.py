"""Dataset ingest adapters.

Each adapter turns some raw source into a valid manifest (see
:mod:`palmguard_ml.manifest`). Add new datasets here; never couple feature or
training code to a specific dataset's layout.
"""
