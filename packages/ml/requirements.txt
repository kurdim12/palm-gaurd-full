# Core pipeline (light — runs on CPU, edge-friendly).
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
joblib>=1.3
pytest>=7.4

# Heavy / optional: needed only for `make train`, `make eval`, `make export`.
# Kept commented so the light path installs fast on cheap hardware. Uncomment to
# train the CNN.
# tensorflow>=2.15

# Optional: fetch TreeVibes directly from Kaggle (needs credentials). Not required
# if you use TREEVIBES_LOCAL with an already-downloaded archive/folder.
# kaggle>=1.6
